# .env file reference
# All values in this file must be filled in accordingly in the .env

# 32-byte hex key used to encrypt provider signing secrets at rest.
# Generate with: openssl rand -hex 32
MASTER_KEY=

# Dashboard login credentials
USER_EMAIL=
USER_PASSWORD=

# 64-byte hex secret used to sign JWT tokens.
# Generate with: openssl rand -hex 64
TOKEN_SECRET=

# Database (app <-> postgres connection)
DB_TYPE=postgres
DB_USER=postgres
DB_PASSWORD=                    # must match POSTGRES_PASSWORD below
DB_HOST=db                      # use "localhost" for local dev without Docker
DB_PORT=5432
DB_NAME=vaulthook

# Docker Postgres container (creates the initial superuser + database)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=              # must match DB_PASSWORD above
POSTGRES_DB=vaulthook

# Goose migrations — uses these env vars automatically.
# Build this string from the DB_* values above. Example:
#   postgres://postgres:yourpassword@db:5432/vaulthook
GOOSE_DRIVER=postgres
GOOSE_DBSTRING=postgres://postgres:<your-password>@db:5432/vaulthook
GOOSE_MIGRATION_DIR=./migrations


# OPTIONAL: sensible defaults are provided below, adjust as needed

# Environment
IS_DEVELOPMENT=false
IS_TESTING=false

# JWT lifetimes
ACCESS_TOKEN_TTL=10             # minutes
REFRESH_TOKEN_TTL=24            # hours

# Request throttling
THROTTLE_MAX_CONCURRENT=100
THROTTLE_MAX_BACKLOG=50
THROTTLE_BACKLOG_TIMEOUT=10     # seconds
MAX_REQUEST_TIME_LENGTH=60      # seconds

# Webhook delivery
MAX_RETRIES=3
RETRY_INTERVAL_SECONDS=15
FORWARD_TIMEOUT_SECONDS=6

# Background workers
TOTAL_QUEUE_WORKERS=8
TOTAL_RETRY_WORKERS=12

# Logging (0=debug, 1=info, 2=warn, 3=error)
LOG_LEVEL=1
